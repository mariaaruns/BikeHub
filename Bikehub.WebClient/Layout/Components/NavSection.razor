@if (!Collapsed)
{
    <div class="nav-section-lbl">@Label</div>
}
else
{
    <div class="nav-section-lbl nav-section-dot">·</div>
}

@code {
    [Parameter] public string Label { get; set; } = "";
    [Parameter] public bool Collapsed { get; set; }
}
